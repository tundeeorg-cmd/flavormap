# Supabase Postgres connection string (separate `flavormap` schema on the shared TunDee instance).
DATABASE_URL=postgresql://user:password@host:5432/postgres

ANTHROPIC_API_KEY=sk-ant-...

# Included in the scraper's User-Agent string so site owners can reach us. Required by §1.8 (rate-limit rule).
SCRAPER_CONTACT_EMAIL=you@example.com
